// 📝 Exercice : Interfaces et Types

// 1. Créez une interface Product avec : id (string), name (string), price (number), inStock (boolean optionnel)
// interface Product {
//   VOTRE CODE ICI
// }

// 2. Créez une interface Category qui étend Product et ajoute : category (string), tags (string[])
// interface Category extends Product {
//   VOTRE CODE ICI
// }

// 3. Créez un type Coordinate pour { x: number, y: number, z?: number }
// type Coordinate = VOTRE CODE ICI

// 4. Créez un type union pour les rôles : "user" | "admin" | "moderator"
// type Role = VOTRE CODE ICI

// 5. Créez un type intersection qui combine User et { isVerified: boolean }
// type VerifiedUser = VOTRE CODE ICI
